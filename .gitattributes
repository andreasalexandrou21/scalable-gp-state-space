# Normalize line endings: store text as LF in the repo, check out native on each OS.
* text=auto eol=lf

# Treat binary assets as binary (no line-ending conversion, no diffs).
*.png binary
*.pdf binary

# Jupyter notebooks: text, stored as LF.
*.ipynb text eol=lf
